- src/extractors/pseudo-extractor.ts now extracts ::marker in addition to ::before/::after by synthesizing measured text from computed marker styles; default list-style-type values like disc/circle/square and ordered-list counters need explicit fallback text because ::marker content is often "normal".
- src/writers/acad-writer.ts DWG hatch-color remap should special-case near-black and near-white true colors to indexed color 7 before serialization; tiny marker fills stayed hard to see when neutral hatches were mapped to fixed ACI colors like 250 instead of the display-adaptive black/white index.
