Hand over fonts to PDFWriter so it can embed fonts somehow.

GetBoxQuads does not return a transformation Matrix for rotation.... For text needed.
But it can be calculated from 2 points

-update readme about embeding fonts to pdf


todo:
- backdrop-filter
  -> https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/backdrop-filter
- fill of arrows heads in dxf
- line-clamp css property (for text)

create a demo html with the features in tests/demos
implement it and test but with firefox, cause only firefox has native box quads api (if we need it).
Use box getBoxQuads API when you need positions and it's possible, so also transformed elements work.
Update readme/feature md files after finish if needed